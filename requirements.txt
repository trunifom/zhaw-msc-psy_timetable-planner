streamlit>=1.32.0
pandas>=2.1.0
# Pinned to 1.x, matching environment.yaml's conda pin - unpinned here
# previously (numpy was removed from this file in an early commit), so
# a plain `pip install -r requirements.txt` - e.g. what Streamlit
# Community Cloud runs - could resolve numpy 2.x, which breaks pandas/
# Streamlit's C-API compatibility with the versions pinned below.
numpy>=1.26.0,<2.0.0
plotly>=5.0.0
pydantic>=2.0.0
pyarrow>=15.0.0
openpyxl>=3.1.0
pytest>=7.0.0